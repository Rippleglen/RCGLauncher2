// Placeholder — full skin management (library, 3D preview, upload) comes when
// the backend skin server replaces imgbb. Keeping the slot here so the tab works.

export default function SkinsView({ user }) {
  return (
    <div className="p-6">
      <h1 className="text-lg font-semibold mb-4 pb-2 border-b border-surface-600">Skins</h1>
      <p className="text-sm text-gray-400">
        Skin management will be available once the skin server is set up.
      </p>
    </div>
  )
}
